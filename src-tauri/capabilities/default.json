{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "主窗口默认权限:核心 IPC + 文件对话框(书库迁移选文件)",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
