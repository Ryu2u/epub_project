# 后端环境变量示例（拷贝为 .env 后按需修改）

# EPUB 文件存放目录
EPUB_STORAGE_DIR=./data/storage

# SQLite 连接 URL（异步驱动）
EPUB_DB_URL=sqlite+aiosqlite:///./data/library.db

# 单文件上传上限 (MB)
EPUB_MAX_UPLOAD_MB=100

# 开发期允许跨域的前端源
EPUB_CORS_ORIGINS=["http://localhost:5173"]